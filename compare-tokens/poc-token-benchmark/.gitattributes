# Force LF for the baseline CLAUDE.md (variants are generated by script)
CLAUDE.md text eol=lf
*.py text eol=lf
*.sh text eol=lf

# PowerShell can have CRLF (native Windows)
*.ps1 text eol=crlf

# Binary files - no conversion
*.jsonl binary
